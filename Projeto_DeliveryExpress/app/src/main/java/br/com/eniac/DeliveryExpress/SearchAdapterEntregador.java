package br.com.eniac.DeliveryExpress;

import android.app.Activity;



import java.util.List;


public class SearchAdapterEntregador extends Activity  {
/*

    protected void onCreate(Bundle savedInstanceState) {
         super.onCreate(savedInstanceState);
         setContentView(R.layout.la);
ListView list = (ListView) findViewById(R.id.listView);

            EditText filter = (EditText) findViewById(R.id.edtText);

            // Cria o Adapter
            final ArrayAdapter<String> adapter = new ArrayAdapter<String>(this, android.R.layout.simple_list_item_1, getResources().getStringArray(R.array.estados));

            // Seta o adapter para o ListView
             list.setAdapter(adapter);

             // Adiciona um TextWatcher
             filter.addTextChangedListener(new TextWatcher() {

         public void afterTextChanged(Editable s) {
             }

         public void beforeTextChanged(CharSequence s,int start, int count, int after) {

        }

         public void onTextChanged(CharSequence s,int start,
        int before, int count) {

             // Aplica o filtro no Adapter
             adapter.getFilter().filter(s.toString());
             }
         });

        }*/
}
