<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/secundaria2"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"

    android:weightSum="1">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:orientation="horizontal">


        <Button
            android:id="@+id/btnClienteEnviar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Confirmar Entregador" />

        <Button
            android:id="@+id/btnClienteVoltar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Voltar" />
    </LinearLayout>

    <ListView
        android:id="@+id/ListEntregID"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:clickable="true"
        android:divider="#FFECEC"
        android:dividerHeight="2dp"
        android:drawSelectorOnTop="true"
        android:fastScrollEnabled="false"
        android:fastScrollStyle="@android:color/background_dark"
        android:focusable="true"
        android:paddingTop="20dp"
        android:layout_weight="0.97"></ListView>

    <ListView
        android:id="@+id/ListMercID"
        android:layout_width="match_parent"
        android:layout_height="176dp"
        android:clickable="true"
        android:divider="#FFECEC"
        android:dividerHeight="2dp"
        android:drawSelectorOnTop="true"
        android:fastScrollEnabled="false"
        android:fastScrollStyle="@android:color/background_dark"
        android:focusable="true"
        android:paddingTop="20dp"></ListView>



</LinearLayout>
